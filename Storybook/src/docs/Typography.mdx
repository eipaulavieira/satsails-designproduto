{/* Typography.mdx */}
import { Meta } from '@storybook/blocks';
import { fontFamilies, fontSizes, fontWeights, pxToRem, flutterTextTheme } from '../tokens/typography';

<Meta title="Fundação/Tipografia" />

export const RemBadge = ({ rem }) => (
  <span style={{
    display: 'inline-flex', alignItems: 'center', gap: 3,
    padding: '2px 7px', borderRadius: 4,
    background: '#f0f0f0', border: '1px solid #e0e0e0',
    fontFamily: 'JetBrains Mono, monospace', fontSize: 10.5, color: '#555',
    whiteSpace: 'nowrap',
  }}>{rem}</span>
);

export const FlutterBadge = ({ sp, textTheme }) => (
  <span style={{
    display: 'inline-flex', alignItems: 'center', gap: 5,
    padding: '2px 7px', borderRadius: 4,
    background: '#E3F2FD', border: '1px solid #90CAF9',
    fontFamily: 'JetBrains Mono, monospace', fontSize: 10.5, color: '#1565C0',
    whiteSpace: 'nowrap',
  }}>
    <span style={{ opacity: 0.6, fontSize: 10 }}>Flutter</span>
    <span>fontSize: {sp}</span>
    {textTheme && <span style={{ opacity: 0.55 }}>·</span>}
    {textTheme && <span style={{ color: '#1976D2' }}>{textTheme}</span>}
  </span>
);

export const SizeRow = ({ px, label }) => {
  const rem = pxToRem(px);
  const theme = flutterTextTheme(px);
  return (
    <div style={{ display: 'flex', alignItems: 'baseline', gap: 10, padding: '14px 20px', background: '#0C0B0D', borderRadius: 8, flexWrap: 'wrap' }}>
      <span style={{ fontFamily: 'Inter, sans-serif', fontSize: px, color: '#fff', minWidth: 52 }}>{px}px</span>
      <RemBadge rem={rem} />
      <FlutterBadge sp={px} textTheme={theme} />
      {label && <span style={{ fontFamily: 'Inter, sans-serif', fontSize: 11, color: '#727272', marginLeft: 4 }}>{label}</span>}
    </div>
  );
};

<div style={{ maxWidth: 860 }}>

<div style={{ marginBottom: 32 }}>
  <p className="ds-section-label">02 / Fundação</p>
  <h1 style={{ fontSize: 32, fontWeight: 700, letterSpacing: '-0.02em', color: '#0C0B0D', margin: '0 0 10px' }}>Tipografia</h1>
  <p style={{ fontSize: 15, color: '#6b7280', lineHeight: 1.7, margin: 0, maxWidth: 560 }}>
    Famílias tipográficas, escala de tamanhos e pesos utilizados em toda a interface da Satsails.
    Cada tamanho exibe o equivalente em <strong>rem</strong> (base 16 px) e a especificação <strong>Flutter</strong> (fontSize em sp + TextTheme Material 3).
  </p>
</div>

---

## Famílias

<div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: 16, marginTop: 24 }}>
  {Object.values(fontFamilies).map(f => (
    <div key={f.token} style={{ padding: '24px', border: '1px solid #e8e8e8', borderRadius: 10 }}>
      <div style={{ fontSize: 11, fontFamily: 'JetBrains Mono, monospace', color: '#9ca3af', marginBottom: 12 }}>{f.token}</div>
      <div style={{ fontSize: 28, fontFamily: f.value, fontWeight: 600, color: '#0C0B0D', marginBottom: 8, letterSpacing: '-0.01em' }}>
        Aa Bb Cc
      </div>
      <div style={{ fontSize: 12, color: '#6b7280' }}>{f.label}</div>
      <div style={{ fontSize: 11, fontFamily: 'JetBrains Mono, monospace', color: '#d1d5db', marginTop: 4, wordBreak: 'break-all' }}>{f.value}</div>
    </div>
  ))}
</div>

---

## Escala de Tamanhos

<div style={{ marginTop: 24, display: 'flex', flexDirection: 'column', gap: 2 }}>
  {fontSizes.map(t => (
    <div key={t.token} style={{ padding: '16px 20px', border: '1px solid #f0f0f0', borderRadius: 8, display: 'flex', flexDirection: 'column', gap: 8 }}>
      {/* linha 1: token + badges */}
      <div style={{ display: 'flex', alignItems: 'center', gap: 8, flexWrap: 'wrap' }}>
        <span style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 11, color: '#9ca3af', minWidth: 100 }}>{t.token}</span>
        <span style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 11, color: '#6b7280' }}>{t.size} / {t.lineHeight}</span>
        <RemBadge rem={t.rem} />
        <FlutterBadge sp={t.flutter.sp} textTheme={t.flutter.textTheme} />
      </div>
      {/* linha 2: amostra */}
      <div style={{ fontSize: t.size, lineHeight: t.lineHeight, fontWeight: 400, color: '#0C0B0D' }}>
        {t.sample}
      </div>
    </div>
  ))}
</div>

---

## Uso Real no Projeto

Frequência de cada tamanho medida diretamente no código do protótipo (`app.html`) e nos componentes React.

### Tier 1 — Crítico (use constantemente)

<div style={{ display: 'flex', flexDirection: 'column', gap: 2, marginTop: 16, marginBottom: 24 }}>
  <SizeRow px={13} label="13× · Card titles, inputs, nav items, labels, tx-name" />
  <SizeRow px={11} label="14× · Descriptions, subtexts, network labels, fees, dates, amounts" />
</div>

### Tier 2 — Muito Usado (use frequentemente)

<div style={{ display: 'flex', flexDirection: 'column', gap: 2, marginBottom: 24 }}>
  <SizeRow px={16} label="7× · Subtitles, section titles, asset titles, input labels" />
  <SizeRow px={14} label="5× · Buttons, social buttons, asset names, selectors" />
  <SizeRow px={15} label="4× · Status time, CTA buttons, select buttons" />
</div>

### Tier 3 — Usado Regularmente

<div style={{ display: 'flex', flexDirection: 'column', gap: 2, marginBottom: 24 }}>
  <SizeRow px={12} label="4× · Error messages, detail headers, taxa bars, dividers" />
  <SizeRow px={18} label="3× · Nav titles, key buttons, scan titles (NavHeader)" />
</div>

### Tier 4 — Hierarquia Alta (para destaque)

<div style={{ display: 'flex', flexDirection: 'column', gap: 2, marginBottom: 24 }}>
  <SizeRow px={24} label="1× · Login subtitle" />
  <SizeRow px={28} label="1× · Afiliado title variant" />
  <SizeRow px={32} label="1× · Email title" />
  <SizeRow px={33} label="2× · Section titles (tipo-conta), balance display" />
  <SizeRow px={48} label="1× · Main headline — tela de login" />
</div>

### Tier 5 — Especial (use raramente)

<div style={{ display: 'flex', flexDirection: 'column', gap: 2, marginBottom: 8 }}>
  <SizeRow px={9}  label="1× · QA labels — Quick Actions somente" />
  <SizeRow px={10} label="2× · Nav labels, disclaimers" />
</div>

---

## Pesos

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: 12, marginTop: 24 }}>
  {fontWeights.map(w => (
    <div key={w.token} style={{ padding: '20px 16px', border: '1px solid #e8e8e8', borderRadius: 8 }}>
      <div style={{ fontSize: 11, fontFamily: 'JetBrains Mono, monospace', color: '#9ca3af', marginBottom: 10 }}>{w.token}</div>
      <div style={{ fontSize: 22, fontWeight: w.value, color: '#0C0B0D', marginBottom: 6 }}>Satsails</div>
      <div style={{ fontSize: 12, color: '#6b7280' }}>{w.name} · {w.value}</div>
    </div>
  ))}
</div>

</div>
